# ####################################### #
# 此文件是本项目的主配置文件，所有配置将被应用 #
# ####################################### #

# Spring的配置
spring:
  # Profile的配置
  profiles:
    # 激活哪些Profile配置
    active: dev

# MyBatis Plus配置
mybatis-plus:
  # 配置SQL语句的XML文件的位置
  mapper-locations: classpath:mappers/**/*.xml